# Secrets for zsh — rendered by chezmoi from 1Password.
# Source: item " Anthropic API" (Private vault) → field "credential".
# Referenced by UUID because the item title has a leading space.
# Edit THIS .tmpl, never the applied ~/.config/zsh/secrets.zsh (mode 600).
export ANTHROPIC_API_KEY={{ onepasswordRead "op://Private/hvvehnhdbwdcjbab3voi7dkezy/credential" | quote }}
